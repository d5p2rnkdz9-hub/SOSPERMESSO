[project]
name = "sospermesso-data-pipeline"
version = "0.1.0"
description = "Scrape e indicizzazione di circolari su immigrazione da aggregatori pubblici"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "selectolax>=0.3.21",
    "python-dateutil>=2.9",
]

[tool.setuptools]
packages = ["sources"]
