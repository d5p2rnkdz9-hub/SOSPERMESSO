---
phase: 55-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _data/documents.js
  - _data/permits.js
  - _data/prassiLocali.js
autonomous: true

must_haves:
  truths:
    - "Editorial notes from 'Info extra su doc rilascio/rinnovo' Notion field appear on document pages (primo and rinnovo) in the built site"
    - "PRASSI_DB_ID is a hardcoded constant in _data/prassiLocali.js, not read from process.env"
    - "Build completes without errors; no existing page content is broken"
  artifacts:
    - path: "_data/documents.js"
      provides: "Correct Notion field name for docNotes"
      contains: "Info extra su doc rilascio/rinnovo"
    - path: "_data/permits.js"
      provides: "Correct Notion field name for docNotes"
      contains: "Info extra su doc rilascio/rinnovo"
    - path: "_data/prassiLocali.js"
      provides: "Hardcoded PRASSI_DB_ID constant"
      contains: "3027355e7f7f80f6957ec3107a5f7aa4"
  key_links:
    - from: "_data/documents.js"
      to: "Notion API"
      via: "property name string match"
      pattern: 'Info extra su doc rilascio/rinnovo'
    - from: "_data/documents.js"
      to: "src/pages/documents-primo.liquid"
      via: "docNotes field -> parseDocNotes filter"
      pattern: "docNotes"
    - from: "_data/documents.js"
      to: "src/pages/documents-rinnovo.liquid"
      via: "docNotes field -> parseDocNotes filter"
      pattern: "docNotes"
---

<objective>
Fix two known data bugs: (1) the Notion field name mismatch that silently drops editorial notes from document pages, and (2) the inconsistent `process.env` usage for `PRASSI_DB_ID` in `_data/prassiLocali.js`.

Purpose: Unblock editorial note content from appearing on the site and align prassi data file with the hardcoded DB ID pattern used by all other data files.
Output: Three modified data files, verified by a clean build.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-bug-fixes/55-RESEARCH.md
@_data/documents.js
@_data/permits.js
@_data/prassiLocali.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Notion field name in documents.js and permits.js</name>
  <files>_data/documents.js, _data/permits.js</files>
  <action>
In `_data/documents.js`, line 120, change the Notion property name string from `"Info extra su doc rilascio"` to `"Info extra su doc rilascio/rinnovo"`. The line currently reads:

```js
const docNotesRichText = page.properties["Info extra su doc rilascio"]?.rich_text || [];
```

Change it to:

```js
const docNotesRichText = page.properties["Info extra su doc rilascio/rinnovo"]?.rich_text || [];
```

Also update the comment on that line (or the line above) to reflect the correct field name.

In `_data/permits.js`, line 71, make the same change. The line currently reads:

```js
const docNotesRichText = page.properties["Info extra su doc rilascio"]?.rich_text || [];
```

Change it to:

```js
const docNotesRichText = page.properties["Info extra su doc rilascio/rinnovo"]?.rich_text || [];
```

Also update the comment on line 70 to reflect the correct field name.

No other changes. Do NOT modify templates -- they already handle `docNotes` correctly via the `parseDocNotes` filter in `documents-primo.liquid` (line 131), `documents-rinnovo.liquid` (line 174), and their EN/FR equivalents.
  </action>
  <verify>
Run: `grep -n "Info extra su doc rilascio" _data/documents.js _data/permits.js`

Expected: Both files show `"Info extra su doc rilascio/rinnovo"` (with the `/rinnovo` suffix). No line should show the old string without `/rinnovo`.
  </verify>
  <done>Both data files reference the correct Notion field name `"Info extra su doc rilascio/rinnovo"`. The `docNotes` field will now be populated from Notion data instead of silently returning empty.</done>
</task>

<task type="auto">
  <name>Task 2: Hardcode PRASSI_DB_ID in prassiLocali.js</name>
  <files>_data/prassiLocali.js</files>
  <action>
In `_data/prassiLocali.js`, line 10, replace:

```js
const PRASSI_DB_ID = process.env.PRASSI_DB_ID;
```

with:

```js
const PRASSI_DB_ID = "3027355e7f7f80f6957ec3107a5f7aa4";
```

This aligns with the pattern used by all other data files:
- `_data/documents.js` line 10: `const DATABASE_ID = "3097355e-7f7f-819c-af33-d0fd0739cc5b";`
- `_data/permits.js` line 20: `const DATABASE_ID = "3097355e-7f7f-819c-af33-d0fd0739cc5b";`

Do NOT change `netlify/functions/submit-prassi.mjs` -- that serverless function correctly uses `process.env.PRASSI_DB_ID` because Netlify Functions have their own env var system.

Also remove the `if (!PRASSI_DB_ID)` guard on lines 23-26 since the value is now always defined. Replace it with a simple comment:

```js
// PRASSI_DB_ID is hardcoded above (consistent with other data files)
// Netlify Functions (submit-prassi.mjs) use process.env for the same value
```
  </action>
  <verify>
Run: `grep -n "PRASSI_DB_ID" _data/prassiLocali.js`

Expected: Line 10 shows the hardcoded string `"3027355e7f7f80f6957ec3107a5f7aa4"`, no `process.env` reference.

Run: `grep -n "process.env.PRASSI_DB_ID" _data/prassiLocali.js`

Expected: No matches (only `process.env.NOTION_API_KEY` should remain).
  </verify>
  <done>`PRASSI_DB_ID` is a hardcoded constant. The data file no longer depends on the `PRASSI_DB_ID` environment variable being set during build.</done>
</task>

<task type="auto">
  <name>Task 3: Verify build passes</name>
  <files>(no files modified -- verification only)</files>
  <action>
Run the full build to confirm no errors were introduced:

```bash
npm run build
```

The build takes ~72 seconds. Check:
1. Build completes with exit code 0
2. No error messages in output related to documents.js, permits.js, or prassiLocali.js
3. Console output shows `[documents.js] Prepared N primo entries, N rinnovo entries` (N > 0)
4. Console output shows `[prassiLocali.js] Fetched N approved practices` (no "PRASSI_DB_ID not set" warning)

If the Notion API key is not available in the local environment, the build will still succeed (graceful degradation returns empty arrays). In that case, verify:
- No crash or error stacktrace
- Build completes and generates _site/ output
  </action>
  <verify>
Build exit code is 0. Check the `_site/` output directory contains document pages:

```bash
ls _site/documenti-*-primo.html | head -5
```

Expected: Multiple document primo pages exist.
  </verify>
  <done>Build passes cleanly. All three bug fixes are applied without breaking any existing functionality.</done>
</task>

</tasks>

<verification>
1. `grep "Info extra su doc rilascio/rinnovo" _data/documents.js _data/permits.js` -- both files match
2. `grep "3027355e7f7f80f6957ec3107a5f7aa4" _data/prassiLocali.js` -- hardcoded value present
3. `grep "process.env.PRASSI_DB_ID" _data/prassiLocali.js` -- no matches
4. `npm run build` exits 0
5. `_site/` contains generated document pages
</verification>

<success_criteria>
1. The Notion field `"Info extra su doc rilascio/rinnovo"` is correctly referenced in both `_data/documents.js` and `_data/permits.js` -- editorial notes will populate `docNotes` on next build with Notion access.
2. `_data/prassiLocali.js` uses a hardcoded `PRASSI_DB_ID` constant, not `process.env`.
3. A full build completes without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/55-bug-fixes/55-01-SUMMARY.md`
</output>
