---
phase: 61-language-template-propagation
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - en/src/pages/permits-en.liquid
autonomous: true
gap_closure: true
requirements:
  - I18N-01
  - I18N-02

must_haves:
  truths:
    - "An EN permit page has a sticky tab bar with 4 badges (What is it / First Issue / Renewal / Local practices) that fixes to top of viewport on scroll"
    - "Clicking any EN tab badge smooth-scrolls to the correct section, accounting for sticky header + breadcrumb + tab bar height"
    - "The CTA 'Do you have more questions?' appears at page bottom after prassi accordion, before Related links — not inline after Q&A"
    - "The 'Remember: within 60 days' alert appears BEFORE the rinnovo checklist, not after it"
    - "The prassi accordion section renders with city names when populated, 'No reports yet' when empty, with English strings throughout"
    - "No extraStyles frontmatter block exists — tab badge CSS comes from shared stylesheet"
  artifacts:
    - path: "en/src/pages/permits-en.liquid"
      provides: "EN permit template with all Phase 60 structural changes"
      contains: "permit-tab-bar"
  key_links:
    - from: "en/src/pages/permits-en.liquid"
      to: "scrollToSection JS"
      via: "inline script after permit-tab-bar div"
      pattern: "function scrollToSection"
    - from: "en/src/pages/permits-en.liquid"
      to: "prassiLocali data"
      via: "prassiLocali[permit.slug]"
      pattern: "prassiLocali\\[permit\\.slug\\]"
    - from: "en/src/pages/permits-en.liquid"
      to: "prassi.js"
      via: "script src after checklist persistence"
      pattern: "src/scripts/prassi.js"
---

<objective>
Apply all Phase 60 structural changes to the EN permit template (`en/src/pages/permits-en.liquid`), closing the 6 verification gaps identified in 61-VERIFICATION.md.

Purpose: The EN permit template was never updated with Phase 60 changes (sticky tab bar, prassi accordion, CTA repositioning, Remember alert repositioning). FR was done in 61-02 correctly. This gap closure brings EN to parity.

Output: Updated `en/src/pages/permits-en.liquid` matching IT/FR structure with English strings.
</objective>

<execution_context>
@/Users/albertopasquero/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albertopasquero/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-language-template-propagation/61-VERIFICATION.md
@.planning/phases/61-language-template-propagation/61-02-SUMMARY.md

Canonical reference (IT template — the source of truth for structure):
@src/pages/permits.liquid

Second reference (FR template — completed correctly in 61-02):
@fr/src/pages/permits-fr.liquid

File to modify:
@en/src/pages/permits-en.liquid

<interfaces>
<!-- The EN template must end up structurally identical to IT (permits.liquid) and FR (permits-fr.liquid). Only text strings differ. -->

Structure order (from IT template):
1. Frontmatter (NO extraStyles block)
2. Breadcrumb
3. Page header (emoji + title only — NO tab badges inside)
4. `{% unless isPlaceholder %}` permit-tab-bar div with 4 badges + scrollToSection JS `{% endunless %}`
5. Placeholder section (if isPlaceholder)
6. Q&A sections loop (NO CTA here)
7. Primo rilascio section
8. Rinnovo section (Remember alert BEFORE checklist)
9. Prassi locali accordion section
10. Checklist persistence script
11. prassi.js script include
12. CTA section (at page bottom)
13. `{% endif %}` (close else)
14. Related links section

Key English strings needed for new sections:
- Tab badges: "What is it", "First Issue", "Renewal", "Local practices"
- Prassi heading: "Local practices"
- Prassi subtitle: "Experiences shared by the community about Police office practices"
- Empty state: "No reports yet"
- Empty state button: "Be the first to share"
- Populated state button: "Add your experience"
- Vote labels: "I confirm:", "I do not confirm:"
- CTA: "Do you have more questions about the permit for {{ permit.tipo }}?" + "Contact us"
- Remember alert: "Remember:" + "The renewal application should be submitted preferably before expiry or at most within 60 days of expiry."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure EN permit template with all Phase 60 changes</name>
  <files>en/src/pages/permits-en.liquid</files>
  <action>
Rewrite `en/src/pages/permits-en.liquid` to match the IT template structure (`src/pages/permits.liquid`) with English strings. Use the FR template (`fr/src/pages/permits-fr.liquid`) as a second reference showing how the translation was done for that language.

**The 7 specific changes (matching VERIFICATION.md gaps):**

1. **Remove `extraStyles` frontmatter block (lines 12-39).** The tab-badge CSS is in the shared stylesheet. Frontmatter should only have: pagination, permalink, layout, title, lang, description, eleventyExcludeFromCollections. No `extraStyles:` key at all.

2. **Extract tab badges from page header into sticky `permit-tab-bar` div.** Currently badges are INSIDE `<section class="section bg-off-white">` page header (line 63). Remove them from there. Instead, AFTER the `</section>` that closes the page header, add:
   ```
   {% unless permit.isPlaceholder == true %}
   <div class="permit-tab-bar" id="permit-tab-bar">
     <div class="container">
       <div class="permit-tab-badges" style="display: flex; flex-wrap: wrap; gap: 0.75rem; justify-content: center;">
         <a href="#cose" class="tab-badge" onclick="return scrollToSection('cose')">What is it</a>
         {% if permit.primoDocuments.size > 0 %}
         <a href="#primo" class="tab-badge tab-badge-primo" onclick="return scrollToSection('primo')">First Issue</a>
         {% endif %}
         {% if permit.rinnovoDocuments.size > 0 %}
         <a href="#rinnovo" class="tab-badge tab-badge-rinnovo" onclick="return scrollToSection('rinnovo')">Renewal</a>
         {% endif %}
         <a href="#prassi-locali" class="tab-badge tab-badge-prassi" onclick="return scrollToSection('prassi-locali')">Local practices</a>
       </div>
     </div>
   </div>
   ```
   Note: 4 badges total (the old template only had 3 — missing "Local practices"). The `onclick="return scrollToSection(...)"` handlers are critical. The gap: is `0.75rem` (not `0.5rem` like the old template had).

3. **Add `scrollToSection` JS function** immediately after the permit-tab-bar div, still inside `{% unless %}...{% endunless %}`:
   ```
   <script>
   function scrollToSection(id) {
     var el = document.getElementById(id);
     if (!el) return true;
     var header = document.querySelector('header') || document.querySelector('.site-header');
     var breadcrumb = document.querySelector('.breadcrumb-bar');
     var tabBar = document.getElementById('permit-tab-bar');
     var offset = 20;
     if (header) offset += header.getBoundingClientRect().height;
     if (breadcrumb) offset += breadcrumb.getBoundingClientRect().height;
     if (tabBar) offset += tabBar.getBoundingClientRect().height;
     var top = el.getBoundingClientRect().top + window.pageYOffset - offset;
     window.scrollTo({ top: top, behavior: 'smooth' });
     return false;
   }
   </script>
   {% endunless %}
   ```

4. **Remove CTA from inside Q&A section.** Currently the CTA `<div class="alert alert-info">` with "Do you have more questions..." is at line 111, inside `<section id="cose">`. Delete it from there entirely. The closing `</div></section>` for the Q&A section should come right after the `{% endfor %}` loop (just like IT template lines 102-105).

5. **Move Remember alert BEFORE rinnovo checklist.** In the rinnovo section, the current order is: submission method callout → attesa-occupazione warning → CHECKLIST → warning alert → Remember alert. The correct order is: submission method callout → attesa-occupazione warning → **Remember alert** → CHECKLIST → warning alert. Move the Remember alert div (currently at line 327-332) to BEFORE the `<!-- CHECKLIST -->` comment. Change its style from `margin-top: 0.75rem` to `margin-bottom: 1rem` to match IT/FR pattern.

6. **Add full prassi accordion section** after the rinnovo section's `{% endif %}`, BEFORE the checklist persistence script. Copy the structure from IT template (lines 378-430) but with English strings:
   - Heading: `📍 Local practices`
   - Subtitle: `Experiences shared by the community about Police office practices`
   - Vote labels: `I confirm:` and `I do not confirm:`
   - Empty state: `📭` + `No reports yet` + button `Be the first to share`
   - Populated button: `Add your experience`
   - The `openPrassiModal` URL should be `'https://sospermesso.it/en/permesso-{{ permit.slug }}.html'` (with `/en/` prefix)

7. **Add CTA at page bottom** after the checklist persistence script and prassi.js include, before `{% endif %}` (closing the else block). Structure:
   ```
   <!-- CTA: DO YOU HAVE MORE QUESTIONS -->
   <section class="section">
     <div class="container" style="max-width: 700px;">
       <div class="alert alert-info">
         <span class="alert-icon">❓</span>
         <div>
           Do you have more questions about the permit for {{ permit.tipo }}?
           <a href="https://form.typeform.com/to/G6YT01Vj" target="_blank" class="btn btn-primary btn-sm" style="margin-left: 0; margin-top: 0.5rem;">
             Contact us
           </a>
         </div>
       </div>
     </div>
   </section>
   ```

Also add `<script src="src/scripts/prassi.js"></script>` right after the checklist persistence script (before the CTA section), exactly as IT and FR templates do.

**CRITICAL: Preserve all existing EN content that is NOT being restructured.** The primo section, rinnovo section (checklist, costs, doc notes, submission method callouts, attesa-occupazione warning), checklist persistence script, and related links section must remain identical to what they are now, just reordered as specified above.
  </action>
  <verify>
    <automated>
node -e "
const fs = require('fs');
const f = fs.readFileSync('en/src/pages/permits-en.liquid', 'utf8');
const checks = [
  [!f.includes('extraStyles'), 'extraStyles removed'],
  [f.includes('permit-tab-bar'), 'permit-tab-bar div present'],
  [f.includes('scrollToSection'), 'scrollToSection JS present'],
  [f.includes('Local practices'), '4th badge present'],
  [f.includes('tab-badge-prassi'), 'prassi badge class present'],
  [f.includes('id=\"prassi-locali\"'), 'prassi section id present'],
  [f.includes('prassi-accordion'), 'prassi accordion markup present'],
  [f.includes('prassiLocali[permit.slug]'), 'prassi data binding present'],
  [f.includes('prassi.js'), 'prassi.js script include present'],
  [f.includes('No reports yet'), 'empty state text present'],
  [f.includes('I confirm:'), 'vote label present'],
];
let pass = 0;
checks.forEach(([ok, desc]) => { console.log(ok ? 'PASS' : 'FAIL', desc); if (ok) pass++; });
console.log(pass + '/' + checks.length + ' checks passed');
if (pass < checks.length) process.exit(1);
"
    </automated>
  </verify>
  <done>
EN permit template has all 6 Phase 60 structural changes applied: sticky tab bar with 4 English badges + scrollToSection JS, CTA at page bottom, Remember alert before checklist, full prassi accordion with English strings, prassi.js included, extraStyles removed. Template structure matches IT and FR.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify structural parity with IT and FR templates</name>
  <files>en/src/pages/permits-en.liquid</files>
  <action>
Run a structural comparison of the EN template against IT and FR to confirm all key structural elements are present and in the correct order. This is a read-only verification task — no file modifications.

Check the following structural order in the EN template:
1. Frontmatter ends at second `---` with no `extraStyles`
2. Breadcrumb bar
3. Page header section (emoji + h1 only, NO tab badges inside)
4. `permit-tab-bar` div OUTSIDE page header, with 4 badges
5. `scrollToSection` function with header/breadcrumb/tabbar offset measurement
6. Placeholder section (conditional)
7. `<section id="cose">` with sections loop, NO CTA inside
8. `<section id="primo">` (conditional)
9. `<section id="rinnovo">` with Remember alert BEFORE `doc-checklist` div
10. `<section id="prassi-locali">` with prassi-accordion
11. Checklist persistence `<script>` IIFE
12. `<script src="src/scripts/prassi.js">` include
13. CTA section with "Do you have more questions"
14. `{% endif %}` closing else
15. Related links section

Also verify no Italian text leaked:
- No "Cos'è", "Primo rilascio", "Rinnovo" (as standalone labels), "Scrivici", "Nessuna segnalazione", "Confermo:", "Aggiungi la tua esperienza", "Sii il primo"
  </action>
  <verify>
    <automated>
node -e "
const fs = require('fs');
const f = fs.readFileSync('en/src/pages/permits-en.liquid', 'utf8');
const lines = f.split('\n');

// Check structural order by finding line numbers of key markers
const markers = {
  'frontmatter-end': lines.findIndex((l, i) => i > 0 && l.trim() === '---') + 1,
  'breadcrumb': lines.findIndex(l => l.includes('breadcrumb-bar')) + 1,
  'page-header': lines.findIndex(l => l.includes('PAGE HEADER')) + 1,
  'tab-bar': lines.findIndex(l => l.includes('permit-tab-bar') && !l.includes('getElementById')) + 1,
  'scrollToSection': lines.findIndex(l => l.includes('function scrollToSection')) + 1,
  'section-cose': lines.findIndex(l => l.includes('id=\"cose\"')) + 1,
  'section-primo': lines.findIndex(l => l.includes('id=\"primo\"')) + 1,
  'section-rinnovo': lines.findIndex(l => l.includes('id=\"rinnovo\"')) + 1,
  'section-prassi': lines.findIndex(l => l.includes('id=\"prassi-locali\"')) + 1,
  'prassi-js': lines.findIndex(l => l.includes('prassi.js')) + 1,
  'cta-bottom': lines.findIndex(l => l.includes('Do you have more questions')) + 1,
  'related': lines.findIndex(l => l.includes('You might also be interested')) + 1,
};

// Check order
const order = Object.entries(markers);
let ordered = true;
for (let i = 1; i < order.length; i++) {
  if (order[i][1] <= order[i-1][1]) {
    console.log('ORDER FAIL:', order[i-1][0], '(L'+order[i-1][1]+')', 'should come before', order[i][0], '(L'+order[i][1]+')');
    ordered = false;
  }
}
if (ordered) console.log('PASS: All structural elements in correct order');

// Check Remember alert is before checklist in rinnovo
const rinnovoStart = lines.findIndex(l => l.includes('id=\"rinnovo\"'));
const rinnovoLines = lines.slice(rinnovoStart);
const rememberIdx = rinnovoLines.findIndex(l => l.includes('Remember:'));
const checklistIdx = rinnovoLines.findIndex(l => l.includes('doc-checklist'));
if (rememberIdx > 0 && checklistIdx > 0 && rememberIdx < checklistIdx) {
  console.log('PASS: Remember alert before checklist in rinnovo');
} else {
  console.log('FAIL: Remember alert not before checklist');
}

// Check no CTA inside cose section
const coseStart = lines.findIndex(l => l.includes('id=\"cose\"'));
const coseEnd = lines.findIndex((l, i) => i > coseStart && l.includes('</section>'));
const coseContent = lines.slice(coseStart, coseEnd + 1).join('\n');
if (!coseContent.includes('Do you have more questions')) {
  console.log('PASS: No CTA inside Q&A section');
} else {
  console.log('FAIL: CTA still inside Q&A section');
}

// Check no Italian text leaks
const italianLeaks = ['Cos.è questo', 'Primo rilascio', 'Scrivici', 'Nessuna segnalazione', 'Confermo:', 'Aggiungi la tua esperienza', 'Sii il primo'];
let leakFound = false;
italianLeaks.forEach(term => {
  if (f.includes(term)) { console.log('LEAK:', term); leakFound = true; }
});
if (!leakFound) console.log('PASS: No Italian text leaks detected');

// Check no extraStyles in frontmatter
const frontmatter = f.substring(0, f.indexOf('---', 4) + 3);
if (!frontmatter.includes('extraStyles')) {
  console.log('PASS: No extraStyles in frontmatter');
} else {
  console.log('FAIL: extraStyles still in frontmatter');
}

// Summary
const allPass = ordered && (rememberIdx < checklistIdx) && !coseContent.includes('Do you have more questions') && !leakFound && !frontmatter.includes('extraStyles');
console.log(allPass ? '\nALL CHECKS PASSED' : '\nSOME CHECKS FAILED');
if (!allPass) process.exit(1);
"
    </automated>
  </verify>
  <done>
EN permit template structural parity confirmed: element order matches IT/FR, Remember alert before checklist, CTA at bottom not inline, no Italian leaks, no extraStyles.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Run Task 1 automated check (11 structural element checks) — all pass
2. Run Task 2 automated check (order, remember placement, CTA placement, Italian leaks, extraStyles) — all pass
3. The EN template structure matches IT and FR exactly (only text strings differ)
</verification>

<success_criteria>
1. `en/src/pages/permits-en.liquid` has no `extraStyles` frontmatter block
2. Tab badges are in a sticky `permit-tab-bar` div outside the page header, with 4 badges including "Local practices"
3. `scrollToSection` JS function is present measuring header/breadcrumb/tabbar heights
4. CTA is at page bottom (after prassi accordion, before Related), not inline in Q&A section
5. Remember alert is before the rinnovo checklist, not after
6. Full prassi accordion section exists with English strings and prassi.js is included
7. No Italian text leaks into EN template
</success_criteria>

<output>
After completion, create `.planning/phases/61-language-template-propagation/61-03-SUMMARY.md`
</output>
