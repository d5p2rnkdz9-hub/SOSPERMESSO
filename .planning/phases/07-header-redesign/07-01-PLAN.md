---
phase: 07-header-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/components.css
  - src/styles/mobile.css
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Header has white background (no teal gradient)"
    - "Menu items are vertically centered within header"
    - "Menu font size is larger and easier to read (1.375rem)"
    - "Logo does not overlap breadcrumb navigation"
    - "Mobile header remains functional with hamburger menu"
  artifacts:
    - path: "src/styles/components.css"
      provides: "Updated header styles with white background"
      contains: "background: #FFFFFF"
    - path: "src/styles/components.css"
      provides: "Updated nav-link font size"
      contains: "font-size: 1.375rem"
    - path: "src/styles/components.css"
      provides: "Reduced logo height"
      contains: "height: 80px"
    - path: "src/styles/mobile.css"
      provides: "Updated mobile logo height"
      contains: "height: 60px"
  key_links:
    - from: ".header"
      to: "white background"
      via: "background CSS property"
      pattern: "background:\\s*#FFFFFF|background:\\s*var\\(--white\\)"
    - from: ".logo-image"
      to: "reduced height"
      via: "height CSS property"
      pattern: "height:\\s*80px"
---

<objective>
Fix header design issues: change background to white, center and enlarge menu items, and reduce logo size to prevent breadcrumb overlap.

Purpose: Close gaps GAP-HEADER-01, GAP-HEADER-02, GAP-HEADER-03 identified in v1.2 milestone audit — creating a cleaner, more functional header.

Output: Updated CSS files with white header, larger centered menu, and properly-sized logo that doesn't block page content.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-header-redesign/07-RESEARCH.md

Key research findings:
- Header is currently 60px with teal gradient (#80CBC4 → #B2DFDB)
- Logo is absolute positioned, 250px tall, overflows into breadcrumbs
- Menu font is 1.2rem, needs to be 1.375rem for better readability
- Breadcrumbs positioned at ~76px from top, get blocked by logo overflow
- Mobile logo is 100px, hamburger menu is 44x44px
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update header background and shadow</name>
  <files>src/styles/components.css</files>
  <action>
Update the `.header` rule in components.css (around line 218-226):

1. Change background from teal gradient to white:
   - FROM: `background: linear-gradient(135deg, #80CBC4 0%, #B2DFDB 100%);`
   - TO: `background: #FFFFFF;`

2. Update shadow for white background (more subtle):
   - FROM: `box-shadow: 0 4px 20px rgba(77, 182, 172, 0.3);`
   - TO: `box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);`

3. Add a subtle bottom border for definition:
   - ADD: `border-bottom: 1px solid rgba(0, 0, 0, 0.06);`

The header should maintain sticky positioning and 60px height.
  </action>
  <verify>
Open index.html in browser, verify:
- Header has white background (not teal)
- Subtle shadow visible below header
- Header still sticks to top when scrolling
  </verify>
  <done>
Header displays with white background, subtle shadow, and bottom border for visual definition.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix menu alignment and increase font size</name>
  <files>src/styles/components.css</files>
  <action>
Update menu styles in components.css:

1. Ensure `.navbar` has proper vertical centering (around line 233-239):
   - Verify: `display: flex;`
   - Verify: `align-items: center;`
   - Verify: `height: 100%;`

2. Ensure `.nav-wrapper` centers vertically (around line 334-339):
   - ADD if not present: `height: 100%;`
   - Ensure: `align-items: center;`

3. Update `.nav-link` font size (around line 350-365):
   - FROM: `font-size: 1.2rem;`
   - TO: `font-size: 1.375rem;`

4. Update `.nav-link` color for better contrast on white:
   - Keep: `color: #1A6B5F;` (dark teal has good contrast)
   - Or adjust to: `color: #1A1A1A;` (black) if teal looks odd on white

5. Update hover state for white background:
   - Consider: `background: rgba(255, 215, 0, 0.15);` for yellow tint on hover
  </action>
  <verify>
Open index.html in browser, verify:
- Menu items are vertically centered in header (not top-aligned)
- Menu text is noticeably larger (compare to previous 1.2rem)
- Hover states work correctly (yellow accent visible)
  </verify>
  <done>
Menu items are vertically centered within header, font size is 1.375rem, hover states work on white background.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reduce logo size and update mobile styles</name>
  <files>src/styles/components.css, src/styles/mobile.css</files>
  <action>
Update logo styles to prevent breadcrumb overlap:

**In components.css:**

1. Update `.logo` positioning (around line 241-248):
   - Keep: `position: absolute;`
   - Update: `top: 5px;` → `top: 50%;`
   - Add: `transform: translateY(-50%);`
   - This centers logo vertically within header

2. Update `.logo-image` height (around line 264-270):
   - FROM: `height: 250px;`
   - TO: `height: 80px;`

   This keeps logo visible but prevents overflow below header.

3. Adjust logo hover state if needed (line 250-253):
   - Keep scale animation but ensure it doesn't cause overflow

**In mobile.css:**

4. Update mobile `.logo-image` height (around line 63-65):
   - FROM: `height: 100px;`
   - TO: `height: 60px;`

5. Update mobile `.logo` positioning (around line 57-61):
   - Update: `top: 50%;`
   - Add: `transform: translateY(-50%);`

6. Verify `.logo-icon-img` remains at 44px for touch target (line 76-78)

**Test breadcrumb visibility:**
After changes, open a detail page (e.g., src/pages/permesso-studio.html) and verify breadcrumbs are fully visible without logo overlap.
  </action>
  <verify>
1. Open index.html - verify logo is smaller (80px) and vertically centered
2. Open src/pages/permesso-studio.html - verify breadcrumbs are fully visible (Home → Database → ...) without logo blocking
3. Open in mobile view (375px) - verify logo is 60px, hamburger menu works
4. Scroll on page - verify logo doesn't overflow and block content
  </verify>
  <done>
Logo reduced to 80px desktop/60px mobile, vertically centered in header, breadcrumbs fully visible on all pages.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. **Visual checks (desktop):**
   - [ ] Header background is white (not teal gradient)
   - [ ] Header has subtle shadow for definition
   - [ ] Logo is ~80px tall, centered vertically
   - [ ] Menu items are vertically centered
   - [ ] Menu font is larger (1.375rem)
   - [ ] Hover states work (yellow background on white)

2. **Breadcrumb visibility:**
   - [ ] Open permesso-studio.html - breadcrumbs fully visible
   - [ ] Open documenti-questura.html - breadcrumbs fully visible
   - [ ] Home link clickable without logo interference

3. **Mobile checks (375px width):**
   - [ ] Logo is ~60px tall
   - [ ] Hamburger menu visible and clickable
   - [ ] Mobile menu slides in correctly
   - [ ] Header maintains white background

4. **Cross-page consistency:**
   - [ ] index.html has white header
   - [ ] src/pages/database.html has white header
   - [ ] src/pages/chi-siamo.html has white header

5. **Gap closure verification:**
   - [ ] GAP-HEADER-01: Header background is white ✓
   - [ ] GAP-HEADER-02: Menu centered and larger ✓
   - [ ] GAP-HEADER-03: Logo doesn't block breadcrumbs ✓
</verification>

<success_criteria>
1. Header background is white (#FFFFFF) with subtle shadow
2. Menu items are vertically centered within 60px header
3. Menu font size is 1.375rem (increased from 1.2rem)
4. Logo height is 80px desktop / 60px mobile (reduced from 250px/100px)
5. Breadcrumbs are fully visible on detail pages without logo interference
6. Mobile header maintains functionality (hamburger menu, touch targets)
7. All 3 gaps (GAP-HEADER-01, 02, 03) are closed
</success_criteria>

<output>
After completion, create `.planning/phases/07-header-redesign/07-01-SUMMARY.md` documenting:
- Changes made to components.css and mobile.css
- Before/after screenshots (if possible)
- Verification results
- Any issues encountered or decisions made
</output>
