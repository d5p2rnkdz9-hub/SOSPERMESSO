---
phase: 05-logo-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - images/logo-icon.svg
  - images/sos-text.svg
  - src/styles/components.css
  - src/styles/mobile.css
  - index.html
autonomous: false

must_haves:
  truths:
    - "Logo displays lighthouse icon with cartoon face and red/white stripes"
    - "S.O.S. text appears with brushstroke style in red-white-red colors"
    - "Permesso text appears in white Quicksand font"
    - "Icon overlaps text for integrated feel"
    - "Logo looks good against teal header"
    - "Mobile shows icon only (no text)"
  artifacts:
    - path: "images/logo-icon.svg"
      provides: "Simplified lighthouse icon with face and eye blink animation"
      contains: "lighthouse"
    - path: "images/sos-text.svg"
      provides: "S.O.S. brushstroke text paths"
      contains: "path"
    - path: "src/styles/components.css"
      provides: "Logo layout styles including overlap positioning"
      contains: ".logo-new"
    - path: "src/styles/mobile.css"
      provides: "Mobile logo styles hiding text below 768px"
      contains: ".logo-text"
  key_links:
    - from: "index.html"
      to: "images/logo-icon.svg"
      via: "img src or inline SVG"
      pattern: "logo-icon"
    - from: "index.html"
      to: "images/sos-text.svg"
      via: "img src or inline SVG"
      pattern: "sos-text"
    - from: "src/styles/components.css"
      to: "index.html"
      via: "class names"
      pattern: "logo-new|logo-icon-wrapper"
---

<objective>
Create new SVG logo assets and implement logo redesign on index.html as proof of concept.

Purpose: Replace complex AI-generated PNG logo with simple, clean SVG lighthouse icon + text that coordinates with teal header from Phase 4.

Output:
- Simplified lighthouse icon SVG (cartoon face, eye blink animation, red/white stripes)
- S.O.S. brushstroke text SVG (red-white-red alternating colors)
- Updated CSS for new logo layout
- index.html updated with new logo markup
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-logo-redesign/05-CONTEXT.md

# Reference files
@src/components/lighthouse.html (existing lighthouse SVG to simplify/adapt)
@src/styles/components.css (current logo styles around line 204-242)
@src/styles/mobile.css (current mobile logo styles around line 57-67)
@index.html (current logo markup around line 36-38)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lighthouse icon SVG</name>
  <files>images/logo-icon.svg</files>
  <action>
Create a simplified lighthouse icon SVG based on the existing src/components/lighthouse.html but reduced to essentials:

**Include:**
- Tower body with red/white horizontal stripes (3 red stripes like current)
- Cupola/top section (dark dome)
- Cute cartoon face on cupola: eyes with highlights, rosy cheeks, small smile
- Light circular/oval background (white or very light, ~15-20% opacity) for grounding on teal header
- CSS animation for eye blink (4s interval, same as current)

**Exclude:**
- Ocean waves
- Platform/base
- Door and windows on tower
- Rotating light beam
- Star on top
- Light room glow effects

**Dimensions:**
- ViewBox: approximately 80x100 or similar compact ratio
- Design for 64px display height on desktop

**Style:**
- Inline styles in SVG for eye blink animation
- Clean, simple paths
- Red stripe color: #FF3B3B (matches existing)
- White/light gray for tower: #FFFFFF with slight gradient

The icon should feel friendly and cartoon-like while being much simpler than the full hero lighthouse.
  </action>
  <verify>
Open images/logo-icon.svg in browser - should display simplified lighthouse with blinking eyes, red/white stripes, no waves/beam/star.
  </verify>
  <done>
logo-icon.svg exists, displays cartoon lighthouse with eye blink animation, compact proportions suitable for header use.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create S.O.S. brushstroke text SVG and update logo styles</name>
  <files>images/sos-text.svg, src/styles/components.css, src/styles/mobile.css, index.html</files>
  <action>
**Part A: Create S.O.S. text SVG (images/sos-text.svg)**

Create SVG with "S.O.S." text as path elements (not font) with brushstroke/hand-painted texture:
- Three letters with periods: S. O. S.
- Colors alternating: S (red #FF3B3B), O (white #FFFFFF), S (red #FF3B3B)
- Periods same color as their preceding letter
- Brushstroke texture effect on paths (slightly rough edges, hand-drawn feel)
- ViewBox sized for inline display (approximately 100x30 or similar)
- No embedded fonts - pure path elements

**Part B: Add Quicksand font to index.html**

Add to the Google Fonts link in index.html head:
- Add Quicksand:wght@500;600;700 to the existing fonts URL

**Part C: Update logo markup in index.html**

Replace current logo markup (lines ~36-38):
```html
<a href="..." class="logo">
  <img src="images/Logo.png" alt="SOS Permesso" class="logo-image">
</a>
```

With new structure:
```html
<a href="#home" class="logo logo-new">
  <span class="logo-icon-wrapper">
    <img src="images/logo-icon.svg" alt="" class="logo-icon-img">
  </span>
  <span class="logo-text">
    <img src="images/sos-text.svg" alt="S.O.S." class="logo-sos">
    <span class="logo-permesso">Permesso</span>
  </span>
</a>
```

**Note:** Root index.html uses `href="#home"`. Subpages in src/pages/ will use `href="../../index.html"` (handled in Plan 02).

**Part D: Update CSS in components.css**

Add new logo styles (keep old .logo styles for backward compatibility, add new .logo-new):

```css
/* New Logo Styles (Phase 5) */
.logo-new {
  display: flex;
  align-items: center;
  gap: 0;
  text-decoration: none;
}

.logo-icon-wrapper {
  position: relative;
  z-index: 2;
  margin-right: -12px; /* Creates 10-15% overlap with text */
}

.logo-icon-img {
  height: 64px;
  width: auto;
  display: block;
  filter: drop-shadow(2px 2px 4px rgba(0, 0, 0, 0.15));
}

.logo-text {
  display: flex;
  align-items: baseline;
  gap: 8px;
  padding-left: 8px;
}

.logo-sos {
  height: 28px;
  width: auto;
}

.logo-permesso {
  font-family: 'Quicksand', sans-serif;
  font-size: 1.6rem;
  font-weight: 600;
  color: var(--white);
  letter-spacing: 0.5px;
}

.logo-new:hover .logo-icon-img {
  filter: drop-shadow(2px 2px 6px rgba(255, 215, 0, 0.4));
}

.logo-new:hover .logo-permesso {
  color: var(--taxi-yellow-bright);
}
```

**Part E: Update mobile styles in mobile.css**

Add mobile styles to hide text below 768px:

```css
/* Mobile Logo - Icon only (Phase 5) */
@media (max-width: 768px) {
  .logo-text {
    display: none;
  }

  .logo-icon-wrapper {
    margin-right: 0;
  }

  .logo-icon-img {
    height: 44px;
  }
}
```
  </action>
  <verify>
1. Open index.html in browser at desktop width - logo shows lighthouse icon overlapping S.O.S. + Permesso text
2. Resize to mobile (<768px) - only lighthouse icon visible, text hidden
3. Hover over logo - subtle highlight effect on icon and Permesso text
4. Verify Quicksand font renders: Open browser dev tools, inspect .logo-permesso element, confirm Computed style shows font-family includes "Quicksand" (not falling back to sans-serif)
  </verify>
  <done>
- sos-text.svg exists with brushstroke S.O.S. in red-white-red
- index.html has new logo markup with Quicksand font loaded
- components.css has .logo-new styles with overlap positioning
- mobile.css hides logo text below 768px
- Logo displays correctly against teal header
- Quicksand font renders properly (verified via dev tools)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify logo design</name>
  <what-built>
New SVG logo with simplified lighthouse icon + S.O.S. brushstroke text + "Permesso" in Quicksand font. Icon overlaps text for integrated feel.
  </what-built>
  <how-to-verify>
1. Open index.html in browser (desktop width 1200px+)
   - Lighthouse icon should appear on left with cartoon face (eyes that blink)
   - S.O.S. should appear in brushstroke style: S (red) O (white) S (red)
   - "Permesso" should appear in white rounded font (Quicksand)
   - Icon should overlap text slightly creating integrated look
   - Logo should look good against teal header gradient

2. Resize browser to mobile width (<768px)
   - Only lighthouse icon should be visible
   - Text (S.O.S. and Permesso) should be hidden
   - Icon should be smaller (around 44px height)

3. Hover over logo
   - Icon should get subtle golden glow
   - "Permesso" text should brighten

4. Check eye blink animation
   - Lighthouse eyes should blink every ~4 seconds
  </how-to-verify>
  <resume-signal>Type "approved" if logo looks good, or describe any issues (icon style, text style, colors, spacing, mobile behavior)</resume-signal>
</task>

</tasks>

<verification>
- [ ] images/logo-icon.svg exists and displays simplified lighthouse
- [ ] images/sos-text.svg exists and displays brushstroke S.O.S.
- [ ] index.html uses new logo markup
- [ ] Quicksand font is loaded
- [ ] Quicksand font renders correctly (verified via browser dev tools)
- [ ] Logo displays correctly at desktop width
- [ ] Logo collapses to icon-only on mobile
- [ ] Eye blink animation works
- [ ] Logo looks good against teal header
</verification>

<success_criteria>
1. LOGO-01: Header displays simple lighthouse icon + "SOS Permesso" text (verified in index.html)
2. LOGO-02: Lighthouse icon retains red/white stripe colors
3. LOGO-03: Icon blends visually with teal header gradient
4. LOGO-04: Logo scales properly (64px desktop, 44px mobile)
5. Mobile shows icon only (text hidden)
6. Eye blink animation preserved from original mascot
7. Quicksand font loads and renders correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05-logo-redesign/05-01-SUMMARY.md`
</output>
