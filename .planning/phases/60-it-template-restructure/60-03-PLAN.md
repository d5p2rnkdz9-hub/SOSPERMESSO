---
phase: 60-it-template-restructure
plan: 03
type: execute
wave: 2
depends_on:
  - 60-01
  - 60-02
files_modified:
  - src/pages/permits.liquid
autonomous: false
requirements:
  - LAYOUT-02
  - LAYOUT-03

must_haves:
  truths:
    - "CTA 'Hai altre domande? Scrivici' appears after all content sections, before Related links"
    - "CTA no longer appears inline after the Q&A Cos'è section"
    - "'Ricorda: entro 60 giorni' alert appears immediately before the document checklist in Rinnovo"
    - "'Ricorda' alert no longer appears after the checklist"
    - "A build completes without errors after all template changes"
  artifacts:
    - path: "src/pages/permits.liquid"
      provides: "Reordered template: CTA at bottom, Ricorda alert before checklist"
      contains: "Hai altre domande"
  key_links:
    - from: "CTA block"
      to: "Related links section"
      via: "DOM order (CTA immediately before Related links)"
      pattern: "Hai altre domande.*Potrebbero interessarti"
    - from: "Ricorda alert"
      to: "Rinnovo checklist"
      via: "DOM order (alert immediately before checklist card)"
      pattern: "Ricorda.*Documenti necessari"
---

<objective>
Relocate the "Hai altre domande? Scrivici" CTA from after the Q&A section to the bottom of the page (before Related links), and move the "Ricorda: entro 60 giorni" alert in the Rinnovo section from after the checklist to before it.

Purpose: The CTA at the bottom of the page catches users who have scrolled through all content. The Ricorda alert before the checklist ensures users see the deadline warning before starting the document preparation.

Output: Modified permits.liquid with reordered template blocks.
</objective>

<execution_context>
@/Users/albertopasquero/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albertopasquero/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/permits.liquid
@.planning/phases/60-it-template-restructure/60-01-SUMMARY.md
@.planning/phases/60-it-template-restructure/60-02-SUMMARY.md

<interfaces>
<!-- Current CTA location: inside #cose section, after Q&A cards (approx lines 112-121) -->
<!-- Current CTA HTML: -->
<div class="alert alert-info">
  <span class="alert-icon">❓</span>
  <div>
    Hai altre domande sul permesso per {{ permit.tipo }}?
    <a href="https://form.typeform.com/to/G6YT01Vj" target="_blank" class="btn btn-primary btn-sm" style="margin-left: 0; margin-top: 0.5rem;">
      Scrivici
    </a>
  </div>
</div>

<!-- Current Ricorda alert location: inside #rinnovo section, AFTER checklist (approx lines 328-333) -->
<!-- Current Ricorda HTML: -->
<div class="alert alert-info" style="margin-top: 0.75rem;">
  <span class="alert-icon">📅</span>
  <div>
    <strong>Ricorda:</strong> La richiesta di rinnovo va presentata preferibilmente <strong>prima della scadenza</strong> o comunque <strong>entro 60 giorni</strong> dalla scadenza.
  </div>
</div>

<!-- Target CTA location: after prassi section, before Related links section -->
<!-- Target Ricorda location: inside #rinnovo, BEFORE the checklist <div class="card"> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move CTA to bottom and Ricorda alert before checklist</name>
  <files>src/pages/permits.liquid</files>
  <action>
Make two block relocations in `src/pages/permits.liquid`:

**Move 1: CTA from after Q&A to before Related links**

1. Find the CTA block inside the `<!-- SECTION: COS'È -->` section. It is the `<div class="alert alert-info">` with "Hai altre domande" text, located after the `{% endfor %}` that closes the Q&A card loop and before `</div></section>`.

2. CUT this entire block (the `<!-- CTA -->` comment + the `<div class="alert alert-info">...❓...Hai altre domande...</div>` block).

3. PASTE it in a new location: AFTER the prassi section closing `</section>` tag and BEFORE the `<!-- RELATED -->` section. Wrap it in its own section for clean spacing:

```html
  <!-- CTA: HAI ALTRE DOMANDE -->
  <section class="section">
    <div class="container" style="max-width: 700px;">
      <div class="alert alert-info">
        <span class="alert-icon">❓</span>
        <div>
          Hai altre domande sul permesso per {{ permit.tipo }}?
          <a href="https://form.typeform.com/to/G6YT01Vj" target="_blank" class="btn btn-primary btn-sm" style="margin-left: 0; margin-top: 0.5rem;">
            Scrivici
          </a>
        </div>
      </div>
    </div>
  </section>
```

Place this between `{% endif %}` (end of else block / non-placeholder content) and `<!-- RELATED -->`. Note: the CTA should appear AFTER the `</script>` for prassi.js and BEFORE the `{% endif %}` that closes the `{% if permit.isPlaceholder == true %}...{% else %}...{% endif %}` block — actually, it should be INSIDE the `{% else %}` branch (since placeholder pages don't need a CTA). The best position is right before the closing `{% endif %}` of the placeholder conditional, after the prassi.js script tag.

Actually, looking at the template structure more carefully:
- Line 77: `{% if permit.isPlaceholder == true %}`
- Line 98: `{% else %}`
- Lines 99-489: all content sections (cose, primo, rinnovo, prassi, checklist script, prassi.js)
- Line 491: `{% endif %}`
- Lines 493-512: Related links section (OUTSIDE the conditional)

So the CTA should go right before line 491's `{% endif %}`, after the prassi.js script tag (line 489). This way it appears for non-placeholder pages, after all content.

**Move 2: Ricorda alert from after checklist to before checklist**

1. Find the Ricorda alert in the Rinnovo section. It is the `<div class="alert alert-info"` with "Ricorda:" text and "entro 60 giorni". Currently located AFTER the checklist progress bar `</div>` and the warning alert, at approximately lines 328-333.

2. CUT this entire `<div class="alert alert-info"...Ricorda...</div>` block.

3. PASTE it BEFORE the `<!-- CHECKLIST -->` comment in the Rinnovo section. Specifically, insert it right after the submission method callout block, after the disputed renewal warning (if present), and right before the `<!-- CHECKLIST -->` comment. Add `style="margin-bottom: 1rem;"` to the alert for spacing before the checklist.

The resulting order in the Rinnovo section should be:
1. `<h2>🔄 Rinnovo</h2>`
2. Submission method callout (kit/questura)
3. Disputed renewal warning (conditional, for attesa-occupazione)
4. **Ricorda alert (MOVED HERE)**
5. Checklist card
6. Checklist progress
7. Warning alert ("I documenti richiesti possono variare...")
8. Notes sections (conditional)
9. Costs section (conditional)

Update the Ricorda alert's margin style to `margin-bottom: 1rem;` (instead of `margin-top: 0.75rem`) since it now precedes the checklist rather than following it.
  </action>
  <verify>
    <automated>cd /Users/albertopasquero/Desktop/TECH/SOSpermesso/Sito_Nuovo && npx @11ty/eleventy --dryrun 2>&1 | tail -5</automated>
  </verify>
  <done>CTA "Hai altre domande?" appears after prassi section and before Related links. It no longer appears inside the Q&A cos'è section. Ricorda alert appears before the Rinnovo checklist, not after it. Template builds without Liquid errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 60 changes</name>
  <files>src/pages/permits.liquid</files>
  <action>
Run the dev server and visually verify all Phase 60 changes in the browser.

All three plans have been applied:
1. Sticky tab bar with 4 badges (Cos'è / Primo / Rinnovo / Prassi locali)
2. Prassi section as compact collapsible accordion
3. CTA relocated to bottom; Ricorda alert moved before checklist

Run: `cd /Users/albertopasquero/Desktop/TECH/SOSpermesso/Sito_Nuovo && npx @11ty/eleventy --serve`

Visit: http://localhost:8080/permesso-lavoro-subordinato.html (or any permit with primo + rinnovo docs)

Verify:
1. **Sticky tab bar:** Scroll down — the 4 tab badges stick below the breadcrumb bar at the top of the viewport.
2. **Tab badge clicks:** Click each badge — it scrolls smoothly to the corresponding section. No full page reload.
3. **Prassi accordion:** Scroll to Prassi locali section. It should show as a compact card, collapsed by default. If it has city entries, click a city name — it should expand. If empty, it shows "Nessuna segnalazione finora".
4. **CTA position:** "Hai altre domande?" appears AFTER the Prassi section, BEFORE Related links. NOT after Q&A cards.
5. **Ricorda alert:** In Rinnovo, "Ricorda: entro 60 giorni" appears BEFORE the document checklist, not after.
6. **Mobile:** Resize to mobile width — tab badges readable, accordion works.
  </action>
  <verify>
    <automated>cd /Users/albertopasquero/Desktop/TECH/SOSpermesso/Sito_Nuovo && npx @11ty/eleventy --dryrun 2>&1 | tail -3</automated>
  </verify>
  <done>Human confirms: sticky tabs visible on scroll, accordion collapses/expands, CTA at bottom, Ricorda before checklist, mobile works.</done>
</task>

</tasks>

<verification>
1. `npx @11ty/eleventy --dryrun` exits with code 0 (no Liquid errors)
2. In the built HTML, "Hai altre domande" appears AFTER "prassi-locali" section and BEFORE "Potrebbero interessarti"
3. In the built HTML, "Ricorda" appears BEFORE "Documenti necessari" in the Rinnovo section
4. Visual verification confirms sticky tabs, accordion, CTA position, and Ricorda position
</verification>

<success_criteria>
- CTA "Hai altre domande?" is at the bottom of the page content, before Related links
- CTA is NOT inside the Cos'è Q&A section
- Ricorda "entro 60 giorni" alert is immediately before the Rinnovo document checklist
- Ricorda alert is NOT after the checklist
- Template builds without errors
- Human visual verification passes
</success_criteria>

<output>
After completion, create `.planning/phases/60-it-template-restructure/60-03-SUMMARY.md`
</output>
